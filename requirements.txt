akshare
tushare
pandas
numpy
matplotlib
mplfinance
pymysql
fastapi
uvicorn[standard]
apscheduler

# 从 PyPI 安装 vn.py；astock/vnpy_runtime/vnpy 仅作为源码审阅参考，不是运行时依赖。
# vn.py 要求 Python >= 3.10；Alpha 策略/回测依赖通过 extra ``alpha`` 安装。
vnpy[alpha]>=4.4.0,<5.0.0
vnpy_xt>=1.0.0
# Linux 本地快速演示（仅模拟行情，不连接真实账户）
vnpy_ctptest>=6.7.2
